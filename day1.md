📘 Day1 学習まとめ（完全理解用）
■ やったことの流れ
React + TypeScriptのプロジェクトをViteで作成
Nodeのバージョン問題を解決（環境修正）
依存関係をリセットして正常化
開発サーバーを起動してブラウザ確認
初期デモ（ロゴ・ボタン）を削除
App.tsxを空にして初期状態にした
frontend / backend にフォルダ分離して構造整理
■ 現在の状態
Reactが正常に動く環境が完成
画面は空（完全スタート状態）
frontendでアプリ、backendは今後用
実務レベルの構成に整理済み
■ コードの核心理解
① Reactの本質

👉 return の中身がそのまま画面になる

② App.tsxの役割

👉 アプリのスタート地点
👉 ここを書き換えると画面が変わる

③ main.tsxの役割

👉 Appを読み込んでブラウザに表示している

■ 環境理解（超重要）
① Node.js

👉 JavaScriptを動かす土台
👉 バージョンが合わないと動かない

② node_modules

👉 ReactやViteなどの「部品」
👉 アプリはこれで動いている

③ 依存関係リセット

👉 壊れた環境を削除して作り直す

■ VSCodeの理解

👉 開いているフォルダ＝プロジェクト
👉 cdしてもVSCodeは変わらない
👉 フォルダを開き直す必要がある

■ 構造理解（重要）
frontend → 画面（React）
backend → API（Laravel）

👉 完全に別物として扱う