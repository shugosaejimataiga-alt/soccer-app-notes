なんかすごいものを学びました。バリデーション処理は入力チェックですが。今回、ガード処理というのを学びました。

なんか編集でplayerを取得する為にidでfindというコードで探そうとしたわけですよ。

としたら、編集でuseStateを使うときにidがないという事もあるという事だから、一部例として書きますが、player.name? || ""って空白の時には""=空白でというコードを出したんです。

だとすると編集機能ではなくなると思ったので、findの後に、if(!player) {return <div>選手が存在しません<div>}というコードを追加しました。

これがAIが出したコードを精査するという事なんですね。